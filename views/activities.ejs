<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Info</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background: #f4f7f9;
            color: #333;
        }

        .container {
            max-width: 800px;
            margin: 40px auto;
            background: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            flex-wrap: wrap;
        }

        .header-text {
            flex: 1 1 60%;
        }

        .header-images {
            flex: 1 1 35%;
            text-align: right;
        }

        .header-images img {
            max-height: 80px;
            margin-left: 10px;
        }

        .b_info,
        .main_content {
            margin-top: 30px;
        }

        .b_info p {
            margin: 5px 0;
            line-height: 1.5;
        }

        .s_name {
            font-size: 30px;
        }

        .collapsible {
            background-color: #777;
            color: white;
            cursor: pointer;
            padding: 18px;
            width: 100%;
            border: none;
            text-align: left;
            outline: none;
            font-size: 15px;
        }

        .active,
        .collapsible:hover {
            background-color: #555;
        }

        .content {
            padding: 0 18px;
            display: none;
            overflow: hidden;
            background-color: #f1f1f1;
        }
        /* Print button CSS */
        /* Print button CSS */
        .print-button {
            display: inline-block;
            background-color: #28a745;
            color: white;
            padding: 10px 20px;
            margin-bottom: 20px;
            font-size: 16px;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .print-button:hover {
            background-color: #1e7e34;
        }

        @media print {
            body {
                zoom: 97%;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            .print-button {
                display: none !important;
            }
        }

        /* Login Button CSS */
        .login-button {
            display: inline-block;
            background-color: #007bff;
            color: white;
            padding: 12px 25px;
            font-size: 16px;
            font-weight: bold;
            text-decoration: none;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .login-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <% if (locals.Student) { %>
        <div class="container">
            <div class="header">
                <div class="header-text">
                    <h1>Computronics</h1>
                    <h3>A Gateway to Computer World</h3>
                    <p>5 No. Serampore Colony, Rajyadharpore (S), Near ESI Hospital,
                        Hooghly, West Bengal</p>
                </div>
                <div class="header-images">
                    <img src="/public/main_logo.png" alt="Main Logo">
                    <img src="/public/QR-Web-MS.png" alt="QR Code">
                </div>
                <hr style="width: 100%; height: 2px; background-color: #007bff; border: none; margin: 20px 0;">
            </div>

            <div class="b_info">
                <p class="s_name"><strong>
                        <%= s_info.name %>
                    </strong></p>
                <p><strong>Father's Name:</strong>
                    <%= s_info.fatherName %>
                </p>
                <p><strong>Date of Admission:</strong>
                    <%= s_info.doa %>
                </p>
                <p><strong>Address:</strong>
                    <%= s_info.address %>
                </p>
                <p><strong>Contact:</strong>
                    <%= s_info.contact %>
                </p>
                <p><strong>Email ID:</strong>
                    <%= s_info.email %>
                </p>
            </div>

            <div class="main_content">
                <h3>Activities:</h3>

                <% const activitySections=[ { label: 'Scholarship' , key: 'scholership' }, { label: 'Award Received' ,
                    key: 'award_recived' }, { label: 'Position Secured' , key: 'position_secured' }, {
                    label: 'Presentation Skill Development Programme' , key: 'psdp' }, { label: 'Seminar & Workshop' ,
                    key: 'seminar_workshop' }, { label: 'Training' , key: 'training' }, { label: 'Others' ,
                    key: 'others' } ]; %>

                    <% activitySections.forEach(section=> { %>
                        <h4>
                            <%= section.label %>
                        </h4>
                        <ul>
                            <% if (activities && activities[section.key] && activities[section.key].length> 0) { %>
                                <% activities[section.key].forEach(item=> { %>
                                    <li>
                                        <%= item.desc %>
                                    </li>
                                    <% }) %>
                                        <% } else { %>
                                            <li>No data available</li>
                                            <% } %>
                        </ul>
                        <% }) %>
            </div>

        </div>
        <center><button class="print-button" onclick="window.print()">🖨️ Print Page</button></center>
        <% } else { %>
            <a class="login-button" href="/">Login</a>
            <% } %>
</body>

</html>