<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <title>Student | <%= Student ? Student.name : "??" %>
    </title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap');

        * {
            font-family: "Roboto", serif;
            font-style: normal;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #f3f4f6;
        }

        .dashboard-container {
            display: flex;
            height: 100vh;
        }

        .sidebar {
            width: 20%;
            background: #16a085;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            color: white;
        }

        .profile-circle {
            width: 100px;
            height: 100px;
            background: #12876f;
            border-radius: 50%;
            margin-bottom: 15px;
        }

        .Student-name {
            font-size: 18px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 30px;
        }

        .sidebar a {
            display: block;
            width: 80%;
            margin: 10px 0;
            padding: 10px;
            font-size: 16px;
            text-align: center;
            background: #12876f;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .sidebar a:hover {
            background: #0f6a5b;
        }

        .main_links-content {
            width: 80%;
            padding: 20px;
        }



        .card {
            display: inline-block;
            background-color: #007bff;
            color: white;
            padding: 12px 20px;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.3s;
            margin: 10px;
        }

        /* Hover effect */
        .card:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
        }

        /* Focus effect for accessibility */
        .card:focus {
            outline: none;
            /* Remove default focus outline */
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.8);
            /* Add a focus outline */
        }

        .nav_container {
            display: flex;
        }

        /* main_links content css */
        .main_links {
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            margin-top: 20px;

        }

        .items {
            width: 180px;
            height: 180px;
            background-color: #12876f;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            font-size: 20px;
            /* You can adjust font size or content here */
            text-align: center;
            transition: transform 0.3s ease;
        }

        .items:hover {
            transform: translateY(-5px);
            cursor: pointer;
        }

        .items a {
            text-decoration: none;
            color: white;
        }


        /* Login Button Css */

        .login-button {
            display: inline-block;
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: bold;
            text-decoration: none;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .login-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <% if (locals.Student) { %>
        <div class="dashboard-container">
            <!-- Sidebar -->
            <div class="sidebar">
                <!-- <div class="profile-circle"></div> -->
                <img src="<%= imageSrc %>" alt="" class="profile-circle">
                <div class="Student-name">
                    <%= Student.name %>
                </div>
                <p>ID: <%= Student.id %>
                </p>
                <a href="/student/changePassword">Change Password &nbsp; <i class="fa-solid fa-key"></i> </a>
                <a href="/student/logout">Log Out &nbsp; <i class="fa-solid fa-arrow-right-from-bracket"></i></a>
            </div>

            <!-- Right side -->
            <div class="main_links">
                <div class="items"><a href="/student/p_info">Personal Information</a></div>
                <div class="items"><a href="/student/activities">Activities</a></div>
                <div class="items"><a href="/student/at_a_glance">At a Glance</a></div>
                <div class="items"><a href="student_result">Result</a></div>
            </div>
        </div>
        <% } else { %>
            <a class="login-button" href="/">Login</a>
            <% } %>
</body>
</html>