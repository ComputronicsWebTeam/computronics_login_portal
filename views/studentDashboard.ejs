<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <title>Student | <%= Student ? Student.name : "??" %>
    </title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap');

        * {
            font-family: "Roboto", serif;
            font-style: normal;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #f3f4f6;
        }

        .dashboard-container {
            display: flex;
            height: 100vh;
        }

        .sidebar {
            width: 20%;
            background: #16a085;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            color: white;
        }

        .profile-circle {
            width: 100px;
            height: 100px;
            background: #12876f;
            border-radius: 50%;
            margin-bottom: 15px;
        }

        .Student-name {
            font-size: 18px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 30px;
        }

        .sidebar a {
            display: block;
            width: 80%;
            margin: 10px 0;
            padding: 10px;
            font-size: 16px;
            text-align: center;
            background: #12876f;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .sidebar a:hover {
            background: #0f6a5b;
        }

        .main-content {
            width: 80%;
            padding: 20px;
        }



        .card {
            display: inline-block;
            /* Makes the link behave like a button */
            background-color: #007bff;
            /* Background color of the button */
            color: white;
            /* Text color */
            padding: 12px 20px;
            /* Padding inside the button */
            font-size: 16px;
            /* Font size */
            text-align: center;
            /* Center the text */
            text-decoration: none;
            /* Remove underline */
            border-radius: 5px;
            /* Rounded corners */
            transition: background-color 0.3s, transform 0.3s;
            /* Smooth hover effects */
            margin: 10px;
            /* Space between buttons */
        }

        /* Hover effect */
        .card:hover {
            background-color: #0056b3;
            /* Darker background on hover */
            transform: translateY(-2px);
            /* Slightly move the button up */
        }

        /* Focus effect for accessibility */
        .card:focus {
            outline: none;
            /* Remove default focus outline */
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.8);
            /* Add a focus outline */
        }

        .nav_container{
            display: flex;
        }

        /* .nav p{
            padding: 10px 0 10px;
        } */

        .head_text h1 {
            text-align: center;
            font-family: sans-serif;
            font-size: 50px;
            color: #194471;
            margin-bottom: 5px;
            /* Reduce space below h1 */
        }

        .head_text p {
            text-align: center;
            font-family: sans-serif;
            font-weight: bold;
            margin-top: 0;
            /* Remove space above p */
            margin-bottom: 0;
            /* Remove space below p */
        }

        .report_head {
            text-align: center;
            font-size: 25px;
            font-weight: bold;
        }

        /* Login Button Css */

        .login-button {
            display: inline-block;
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: bold;
            text-decoration: none;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .login-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <% if (locals.Student) { %>
        <div class="dashboard-container">
            <!-- Sidebar -->
            <div class="sidebar">
                <div class="profile-circle"></div>
                <div class="Student-name">
                    <%= Student.name %>
                </div>
                <p>ID: <%= Student.id %>
                </p>
                <a href="/student/changePassword">Change Password &nbsp; <i class="fa-solid fa-key"></i> </a>
                <a href="/student/logout">Log Out &nbsp; <i class="fa-solid fa-arrow-right-from-bracket"></i></a>
            </div>

            <!-- Main Content -->
            <div class="main-content">
                <div class="nav_container">
                    <img src="/main_logo.png" alt="main_logo">
                    <div class="head_text">
                        <h1>ComputronicS</h1>
                        <p>A Gateway To Computer World</p>
                    </div>
                </div>

                <hr style="height: 5px; width: 100%; background-color: #0f6a5b;">

                <p class="report_head">Evalution Report Session(2024-25)</p>

                <!-- Result Links -->
                <a class="card" href="/public/ITKID-24-25_RESULT.pdf">IT-KID(5th-9th)</a>
                <a class="card" href="/public/FINAL-MARKS_CA-XI-XII-24-25.pdf">COMA(XI-XII) & CS-XI</a>
                <a class="card" href="/public/DCA_B3-SEM-I_TABULATION SHEET.pdf">DCA-B3-SEM-I</a>
                <a class="card" href="/public/DCA_B2-SEM-III_TABULATION SHEET.pdf">DCA-B2-SEM-III</a>
            </div>
        </div>
        <% } else { %>
            <a class="login-button" href="/">Login</a>
            <% } %>
</body>

</html>